# tools/loadgen 專用依賴——刻意不併入 tools/requirements.txt:
# ci.yml 的依賴白名單不裝本檔,locust 生態不進主 CI(負載工具只在本機/專屬機跑)。
locust>=2.46.0,<3
PyJWT>=2.13.0,<3
# mqtt_fanin.py(與 tools/requirements.txt 同款,standalone 安裝用)
paho-mqtt>=2.1.0,<3
protobuf>=7.35.1,<8
