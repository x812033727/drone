# mission-svc 執行依賴(釘版:對齊 cloud/fleet_svc、cloud/log_svc)
fastapi==0.139.0
uvicorn==0.51.0
asyncpg==0.31.0
pydantic>=2.13.4,<3
aiomqtt>=2.5.1,<3    # 派遣發布 + 進度消費(對齊 cloud/ingest)
protobuf>=7.35.1,<8  # drone-proto 契約組裝/解析
pyjwt[crypto]>=2.13.0,<3  # JWT 認證(HS256 dev / JWKS RS256 生產)
