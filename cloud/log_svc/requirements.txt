# log-svc 執行依賴(釘版:雲端服務映像求可重現建置)
fastapi==0.139.0
uvicorn==0.51.0
python-multipart==0.0.32   # FastAPI UploadFile(multipart)解析
asyncpg==0.31.0
pyjwt[crypto]>=2.9,<3      # JWT 認證 + RBAC(HS256 dev / JWKS RS256 生產,對齊 fleet-svc)

# tools/ulog_report.py 子程序依賴(Dockerfile COPY tools/ulog_report.py 進映像)
pyulog==1.2.3
numpy==2.2.6
