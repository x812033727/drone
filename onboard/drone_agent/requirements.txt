# drone-agent 執行依賴
mavsdk>=3.15.3
aiomqtt>=2.5.1,<3
protobuf>=7.35.1,<8
httpx>=0.28.1,<1   # S20 ULog 上傳(async multipart)

# 契約生成碼 drone-proto(repo 內套件,另行安裝):
#   pip install -e ../../interfaces/proto/gen/python
